# Android Release Signing Configuration
# Copy this file to key.properties and fill in your values
# DO NOT commit key.properties to version control!

storePassword=your_keystore_password
keyPassword=your_key_password
keyAlias=prosepal
storeFile=/path/to/your/keystore.jks

# To create a new keystore:
# keytool -genkey -v -keystore prosepal-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias prosepal
