Privacy & trust messaging — the quiet promise that the user's words are theirs. Sage-tinted, never alarmist.

```jsx
<TrustNote
  title="Your writing stays yours"
  body="Drafts are processed privately and never used to train models."
  points={["On-device drafts by default", "No training on your text", "Delete anything, anytime"]}
/>

<TrustNote inline title="Private mode is on" />
```

Use the card form on onboarding/paywall; `inline` for a slim in-context reassurance.
