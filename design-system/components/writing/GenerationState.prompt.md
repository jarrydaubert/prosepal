The generation/loading state. Calm, never spinner-frantic — a breathing clay orb, a human status line, and shimmering placeholder lines in the draft's shape.

```jsx
<GenerationState label="Reading your note…" lines={4} />
```

Rotate the label through warm, specific phrases ("Finding the right words…", "Keeping your voice…"). Replace with a `DraftCard` when ready.
