The iOS toggle — clay when on, springy knob. Controlled.

```jsx
<Switch checked={privateMode} onChange={setPrivateMode} label="Private mode" />
```

Pair inside a `ListRow` trailing slot for settings. Always provide `label`.
